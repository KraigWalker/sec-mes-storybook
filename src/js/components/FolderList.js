import React from 'react';
import { FolderListContainer } from 'document-management-web-ui';

export const FolderList = (props) => (
    <div className="web-ui-components centralised-container">
        <FolderListContainer {...props} />
    </div>
)