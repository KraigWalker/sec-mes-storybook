import { Meta } from '@storybook/addon-docs';
import { FloatingActionButton } from './';

<Meta title="FloatingActionButton/Default" component={FloatingActionButton} />

# FloatingActionButton

A Primary Action Button, used in the app to allow the Customer to create a new message.

## Accessibility Tasks
